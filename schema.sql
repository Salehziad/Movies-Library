-- DROP TABLE IF EXISTS recipe;

CREATE TABLE IF NOT EXISTS movie (
    id varchar(255),
    title varchar(255),
    release_date varchar(255),
    poster_path varchar(255),
    overview varchar(555)
);